import { FormatChamadoPipe } from './format-chamado.pipe';

describe('FormatChamadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatChamadoPipe();
    expect(pipe).toBeTruthy();
  });
});
